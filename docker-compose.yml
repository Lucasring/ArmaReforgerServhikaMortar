version: "3"
services:
  frontend:
    build: ./mortar-app-frontend
    image: mortar-frontend:latest
    container_name: mortar_frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    image: mortar-backend:latest
    container_name: mortar_backend
    expose:
      - "8000"